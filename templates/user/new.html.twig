{% extends 'navbar.html.twig' %}

{% block title %}Nouvel Utilisateur{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-6 fw-bold mb-0"><i class="fas fa-user-plus text-primary me-2"></i>Creer un utilisateur</h1>
        <a href="{{ path('user_index') }}" class="btn btn-outline-secondary">Retour a la liste</a>
    </div>

    {{ include('user/_form.html.twig') }}
</div>
{% endblock %}
