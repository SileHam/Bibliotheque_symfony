{% extends 'navbar.html.twig' %}

{% block title %}Modifier Utilisateur{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-6 fw-bold mb-0"><i class="fas fa-user-gear text-primary me-2"></i>Modifier l utilisateur</h1>
        <a href="{{ path('user_index') }}" class="btn btn-outline-secondary">Retour a la liste</a>
    </div>

    {{ include('user/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    <div class="mt-3">
        {{ include('user/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
